# Rust
/target/
**/*.rs.bk
*.pdb
Cargo.lock

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Configuration (exclude local configs, keep templates)
/config.yaml
/config.toml
only1mcp.yaml
only1mcp.toml
/.only1mcp/
!config/templates/
solo.yaml

# Logs
*.log
/logs/

# Environment
.env
.env.local
.env.*.local

# Test artifacts
/coverage/
*.profraw
*.profdata

# Benchmarks
/target/criterion/

# Documentation build
/target/doc/
/book/

# Temporary files
/tmp/
*.tmp
*.temp

# Session memory (local only)
CLAUDE.local.md

# Development logs (personal notes)
dev-logs/*
!dev-logs/README.md

# OS
Thumbs.db
.DS_Store

# Build artifacts
*.exe
*.dll
*.so
*.dylib

# Local salvage of scratch material, one dated folder per rescue
# (salvaged/YYYYMMDD/). This is a public repository; working notes, release
# drafts and fetched research are kept on disk for reference but are
# deliberately never committed or pushed.
salvaged/
